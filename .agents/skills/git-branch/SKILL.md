---
name: git-branch
description: create a git branch with a nice name
---

## Usage Restrictions (IMPORTANT)

Scope: restricted to generating a git branch name. Do not run any other git operations.

- DO NOT auto-trigger this skill based on conversation context.
- ONLY invoke if the user explicitly uses the command "/git-branch".
- If the task seems relevant but the command is not used, simply inform the user the tool is available.

Please create a git branch, the name should be made of of 3 words,
separated by a dash, no rude words allowed, please give me 3
suggestions and ask me which one i prefer, one option should also
be for you to do it again. The 3 words generated should cover
these 3 topics, ["adjectives", "animals", "colors"], be
imaginitive and fancifull
